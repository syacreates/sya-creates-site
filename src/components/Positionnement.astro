---
const piliers = [
  {
    numero: '01',
    emoji: '🎯',
    titre: 'Expertise UX/UI',
    texte:
      "Dix ans de pratique produit : interviews, tri de cartes, wireframes, tests d'utilisabilité — de la recherche utilisateur au design d'interface, dont plusieurs années sur des sujets de transformation RH et d'innovation au sein de grandes organisations.",
  },
  {
    numero: '02',
    emoji: '⚡',
    titre: 'AI-assisted development',
    texte:
      "Le code va aussi vite que le design. Les outils d'IA accélèrent le prototypage et l'intégration, pour livrer des interfaces fonctionnelles en jours, pas en mois.",
  },
  {
    numero: '03',
    emoji: '🚀',
    titre: "Rapidité d'exécution",
    texte:
      "Moins d'allers-retours, moins d'incertitude : une méthode resserrée qui va du cadrage à la mise en ligne sans perdre en qualité ni en clarté.",
  },
];
---

<section class="positioning" id="positionnement">
  <div class="positioning__inner">
    <span class="eyebrow-pill">
      <span class="eyebrow-pill__dot"></span>
      Positionnement
    </span>

    <h2 class="positioning__title">
      Le design comme métier, <em>l'IA comme accélérateur.</em>
    </h2>

    <p class="positioning__intro">
      Dix ans à concevoir des produits utiles pour des organisations exigeantes m'ont appris
      qu'un bon design ne se résume pas à une interface soignée : c'est une méthode. Le
      développement assisté par IA ne remplace pas cette méthode — il la met au service d'une
      exécution plus rapide, sans rogner sur la qualité.
    </p>

    <div class="positioning__pillars">
      {
        piliers.map((pilier, index) => (
          <div
            class="pillar glass-card reveal-item flip-card"
            style={`--i: ${index}`}
            tabindex="0"
            role="button"
            aria-expanded="false"
            aria-label={`${pilier.titre} — appuie pour voir le détail`}
          >
            <div class="flip-card__inner">
              <div class="flip-card__front">
                <div class="pillar__header">
                  <span class="pillar__numero">{pilier.numero}</span>
                  <span class="pillar__emoji emoji-live" aria-hidden="true">{pilier.emoji}</span>
                </div>
                <h3 class="pillar__titre">{pilier.titre}</h3>
              </div>
              <div class="flip-card__back">
                <p>{pilier.texte}</p>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .positioning {
    position: relative;
    padding: 96px 24px;
  }

  .positioning__inner {
    max-width: 1180px;
    margin: 0 auto;
  }

  .positioning__title {
    margin-top: 20px;
    font-size: clamp(1.6rem, 1.2rem + 1.6vw, 2.5rem);
    line-height: 1.2;
    font-weight: 600;
    color: var(--color-ink);
    max-width: 28ch;
  }

  .positioning__title em {
    font-style: italic;
    font-weight: 500;
    background: var(--gradient-accent);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .positioning__intro {
    font-family: var(--font-body);
    font-size: 1.0625rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    max-width: 62ch;
    margin-top: 24px;
  }

  .positioning__pillars {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-top: 56px;
  }

  .pillar {
    min-height: 240px;
  }

  .pillar .flip-card__front {
    padding: 32px;
  }

  .pillar .flip-card__back {
    padding: 28px;
  }

  .pillar .flip-card__back p {
    font-size: 0.875rem;
  }

  .pillar__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 20px;
  }

  .pillar__numero {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--gradient-accent);
    color: #ffffff;
    font-family: var(--font-mono);
    font-size: 13px;
  }

  .pillar__emoji {
    font-size: 1.5rem;
  }

  .pillar__titre {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-ink);
    margin-bottom: 12px;
  }


  @media (max-width: 860px) {
    .positioning {
      padding: 64px 20px;
    }

    .positioning__pillars {
      grid-template-columns: 1fr;
    }
  }
</style>
